# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

- `npm run dev` — dev server on `localhost:4321`
- `npm run build` — production build to `dist/`
- `npm run preview` — preview the built site locally
- `npm test` - the sheet geometry check (`src/lib/sheet.test.ts`), the ink plate's layout check (`src/lib/drops.test.ts`) and the writer core's test (`src/writer/entry.test.ts`), all under `node --test`. No lint or typecheck scripts exist. Verify changes with `npm test` and `npm run build`; `.claude/check.sh` runs both for the stop and commit gates, and CI runs the same two before Pagefind.

Node 24 LTS runs it: `.nvmrc` pins the line, `nvm use` picks it up, and CI reads the same file. `engines` in `package.json` is only the floor Astro itself needs. Deploy is automated — never run a manual deploy step.

## Architecture

Astro 7 static site, no JS framework islands (no React/Vue/Svelte). Pages are `.astro` files; rich content is MDX.

- **Path alias**: `@/*` → `src/*`
- **Content collections** are defined in `src/content.config.ts` with a `glob` loader. The Zod schemas there are the source of truth for frontmatter — when adding a new field, update the schema first.
- Three collections: `blog` (md/mdx, has `draft` field), `projects` (yaml, one file per project, no body), `interests`. Blog posts with `draft: true` are filtered out of listings.
- `src/content/` files are read via Astro's content collections API (`getCollection`), not raw filesystem reads.
- Routes live in `src/pages/`; layouts (`Base`, `Page`, `BlogPost`) in `src/layouts/`; reusable UI in `src/components/`. The deep-water figures are one component each under `src/components/figures/`, the chart marginalia one each under `src/components/marks/`; `Drift.astro` and `ChartMarks.astro` mount them and carry the behaviour. The blog reads on paper: the log at `/blog` and every entry sit in the surface slot, and only earlier/later, topics, search and the feed sit under the waterline.
- **The log is off.** `SHOW_BLOG` in `src/lib/log.ts` is `import.meta.env.DEV`, so the blog prints under `astro dev` (the chart table still previews a draft against the real page) and stays off the built site: no nav link, no button on home, no recent-remarks section, no entry or tag pages, an empty feed and no `<link rel="alternate">` to it. `/blog` itself is a file-based route and still emits, so it is built `noindex` (out of Pagefind) and filtered out of the sitemap in `astro.config.mjs`. Set `SHOW_BLOG` to `true` and drop that sitemap filter to publish the log again.

## Styling

- **Tailwind CSS v4** wired in via `@tailwindcss/vite` (not PostCSS). All custom CSS lives in a single file: `src/styles/global.css`.
- Uses v4 syntax — `@import "tailwindcss"` and `@plugin`, **not** v3's `@tailwind base/components/utilities` directives.
- Theme tokens are defined in a `@theme {}` block — use these CSS variables, not raw color values.
- **Two zones.** The page opens on chart paper (`.zone-surface`) and crosses a waterline once into deep water (`.zone-deep`). `.zone-deep` remaps the semantic tokens (`--color-text`, `--color-surface-*`, `--color-border-*`, `--color-accent-soft`), so a Tailwind utility like `text-text-muted` follows the zone on its own. Style with semantic tokens, never with the fixed anchors (`--color-paper`, `--color-abyss`, `--color-cream`, `--color-ink`) unless the value must stay constant across both zones.
- Accent is `--color-buoy` (`#E4572E`). On paper it measures 3.12:1, so restrict it there to large text, rules and marks — never body copy. `--color-foam` (`#7FD1C0`) is reserved for live/dynamic values and is deep-zone only. The one exception is a code block: Shiki runs on its `css-variables` theme and `.prose pre` paints it as water (abyss ground, cream text, foam strings, buoy keywords) wherever it prints, so the anchors and foam apply inside it even on paper.
- Type: `--font-sans` is Archivo variable. Use `.display-name` for headings (expanded, fluid `clamp()`), `.label` for condensed uppercase section lettering. `--font-mono` is IBM Plex Mono, for data and small caps labels only.
- The portrait is a chart inset (`.portrait-inset`): a ruled frame with printer's corner ticks and a coordinate caption. The ring it replaced is gone.
- Orange dots are two systems, not three: the rule on a section heading ends in one, and the soundings printed on the paper are the other. The log-book rail (`.rail`) is one hairline down a section; `.rail__fix` on an entry's date line pins it to the rail with the depth rail's marker in miniature, so the CV reads as soundings.
- The passage plot (`Passage.astro`, class `.passage`) prints on home under the water and on the about page beside the story: Mercator, rhumb lines between fixes, no land. Port positions are the country's port of call to the nearest half degree. Below `sm` the graticule labels go and only the key ports keep their names at a larger size; the prose on the about page lists every port so nothing is lost with them. The track stroke does not scale, so it holds the swell line's weight at every width.
- A project is a chart sheet (`Sheet.astro`, class `.sheet`): the stack names are marks on all four frames (split by stack size in `src/lib/sheet.ts`, so bearings arrive from opposing quadrants), each drawn as its logo with the name beside it and a bearing run in to the fix, and the fix is the project. Hovering a mark lights its bearing. Logos are monochrome 24x24 marks in `src/lib/logos.ts` (Simple Icons where one exists, hand-drawn where not); a name without a row there prints as a plain tick. The geometry is in `src/lib/sheet.ts` and its test is the proof that marks cannot collide and no bearing crosses another mark at the schema's caps (sixteen characters a name, eight names); change a range, the icon or the name size there and rerun `npm test`. The fix and the graticule are hashed from the title so no two sheets share geometry. Every bearing carries its degrees, true from the fix to the mark. The title block is HTML over the plate's bottom-left corner so it wraps instead of truncating; the sheet number prints under the frame as the inset's caption. Two plates are in the markup and CSS shows one: the landscape `WIDE` plate from `sm` up, the portrait `TALL` plate below it with logos only, because a landscape sheet at phone width is a strip. On a phone a mono line under the description carries the names with their logos; above `sm` that line is for screen readers only. The observer that arms the draw-on fires at a quarter of the sheet, not the passage's third: a sheet with its text is taller than a plot. Order is the `rank` field, hand-set per entry and lowest first, because the sheet worth reading first is rarely the one that finished last; the home page prints rank 1. Sheets print in series by `org` (`src/lib/orgs.ts`: CCT, TU Berlin, own account, client), each opened by `Series.astro` with the publisher's crest in a ruled inset: CCT's raster mark through a CSS mask, TU Berlin's logo inline, the anchor for own work, and a dashed "crest withheld" frame for client work. A series prints where its first sheet would have, and the caption numbers the sheet within it, so the home page's rank 1 sheet is sheet 01 of its own series. One column, no domain sections within a series: a grid of plates is a grid of cards. Each mark carries a tier read off the commit history when the sheet was drawn: the entry's `core` list is where most of the changed lines went, `touched` is what was wired up and left alone, and the rest is the middle. The bearing to a core mark is drawn heavy and to a touched one barely there, and the logo and name take the same ink. The key prints once, the way a chart prints its legend once, as a caption in the water at the head of the sheets, just above the first series; on a phone the list under the description groups the names by tier as well. It sat on the paper beside the index twice, above it and then below it under its own heading, and every critic read it as the index's legend, and the index has no weights. The paper opens on the ink (`Ink.astro`, classes `.ink`, `.blot`): one blot per stack name with a sheet's work or more behind it across every sheet, its size the weight of use (`ink()` in `src/lib/projects.ts`: a core name counts one sheet, one in the middle half, one touched in passing a quarter; anything under one sheet is a droplet, printed at four to nine units (its weight and a hash, so the bottom of the scale runs from a speck to a drop) without a logo, since a lighter blot has no room for one, or a word, since a plate of what was touched in passing is not what the top of the page is for, but printed, because a spill has a bottom to its size scale and a plate that stops at the smallest logo is a plate of pebbles: a droplet is landed and shoved like the rest, never opens, is never hit and is not read out; the radius is eighteen units times the weight to the power 0.45, from eighteen for a sheet's work to fifty-eight at the cap: the floor is what a logo needs to be recognised (at twelve units it was a smudge; at twenty the tall plate had no room for the test's heavier spill), the main stacks plainly bigger than the rest is all the scale has to say, and the spread between them matters less than reading the logos). It is spilled on the paper as if by accident: no frame, no heading, no key, no caption, nothing lettered, and the canvas is clear where there is no ink and bleeds past the field into the column's margins, so the page's own paper is the plate's and a speck thrown past the edge is not cut straight. Only the logo is reversed out of the ink, nine tenths of the radius across so the lightest blot keeps a rim of ink (at eleven it was an icon in a chip): the reader has the sheets for names. The story of the spill is told in the chart's own hand, in two marks over the canvas and under the blots (`Well.astro` and `Quill.astro` in `src/components/marks/`, hairline and unfilled like the rest of the marginalia): the inkwell that went over in the swell, a squat faceted glass on its side with its stopper rolled off, printed at a third over its drawing (`scale` on the mark; at one to one it was too small a bottle for the ink, and at a fifth up the critic still said so), ink specked over the glass and the ring its lip printed where the mouth came down before it rolled, broken where it did not touch and smeared where it dragged (the print is what says it went over rather than being laid down), its mouth toward the heaviest blot, and the shader runs a tongue of ink from the mouth into that blot's pool (`u_run`, following the blot as it lands and as it is shoved; necked where it leaves the mouth, swelling where it lands, each edge with a profile of its own, since parallel sides were a ruled bar), and the quill that fell nib first into the pool and skidded to where it lies, its nib inked and lying in a speck of ink, the vane starting well up the shaft with a few barbs well spaced (a vane the length of the feather, hatched close, was a leaf and the heaviest ink after the pools), and the shader drawing the skid out of the pool toward the nib, as wide as a nib where it left the ink and thinning to nothing, with a dry skip where the nib bounced and a last comma short of the nib (`u_nib`; a line of one width from pool to nib was a cable). The well lies well to the left of the column so the spill has the column to fan into; at its middle the spill sat in the right half. On the tall plate it lies at the top, tipped down the column, so the spill runs down from its mouth the way gravity and a tall plate agree; at the side, the ink stood a screen above the mouth. Each mark is drawn in its own box in plate units and placed by an origin with a turn about it (`WELL` and `QUILL` in `src/lib/drops.ts`, per plate; the well's origin is its mouth and the quill's its nib, so the ink runs from where the one stays and the skid ends where the other does), keeps its paper as capsules in its own units that `placed()` turns onto each plate (a box round a feather lying askew was mostly empty paper), the layout keeps off them, the shove in the browser treats them as things that do not move so a pool opening over the bottle is moved off it, by just enough that the bottle's outline is off the ink, since every unit of that slides the open pool away from the pointer, and the test proves no blot lies on either. Hover a blot (keyboard focus, or a tap, which holds it; a click does nothing a hover does not) and it opens: it swells to the radius `opened()` in `src/lib/drops.ts` gives it, which is what its ring of domain words needs at the size they letter (`wordSize()`, ten units in a small pool up to twelve in a wide one; a single word sits square under the logo on a tighter ring with no jitter, since one word off plumb is a word set crooked, so the pool is what one word needs and not a ring with one word on it) and never less than two fifths again, so the lightest blot open is still smaller than the heaviest at rest and the size keeps its meaning; it shoves its neighbours clear on a spring (a cap height and more, plus a share of both radii for the rims' warp and lobes, so the open one stands clear of what it shoved: closer, the two were one mass and no one could tell which had opened) and they pass the shove on, twenty rounds a frame so a chain blocked by a mark still resolves, while two shoved together keep only the layout's own least gap and may join the way pooled ink does; it merges with what it meets a little more readily than the rest do with each other so a shoved neighbour pulls away with a neck, slow low waves run through it from the pointer, and the domains it was used in appear as spaced capitals in paper in a ring round the logo, the ring turned a hashed way off the cardinal points, never less than twenty degrees (four words at twelve, three, six and nine read as a compass rose, and the first ring's turn ran through twelve), each a hashed few degrees off its bearing and a visible share off the ring, more with three words or more, so the words sit at plainly different radii and the ring is as irregular as the rim. Two words sit above and below the logo within ten degrees of plumb, where the pool is widest for them: turned further they lettered out over the rim on the phone, and the chord the pool opens to (`CHORD`) allows for the tilt, the jitter and a pool's crust. A small blot opened keeps its drop's own harmonics, scaled up with it, and takes a pool's crust at the size it is now: with both scaled down as it opened it opened into a smooth disc, and the one interactive moment was the moment the ink turned into a button. The hover is a hit test on the ink as drawn, run on every pointer move, not enter and leave events on the buttons: a blot shoved out from under a still pointer fires no leave, and stayed open. The shove is worked out on where the blots should be and every blot springs to its own place, damped near critical; shoving the positions directly had the constraint fighting the spring every frame, and the plate flickered. What a blot pinned at the plate's edge cannot take of a shove, the open one takes, so a pool at the edge moves in. The heaviest blot once opened by itself after the landing and closed again, to show what the plate does without a caption; it read as a hover nobody made, and went. Position is two readings. The quieter is the domain: a blot is pulled from the plate's middle toward its domains' anchors on an ellipse round the plate, spread over a fan of two hundred degrees ahead of the well's mouth so no domain's stacks are asked to lie behind the bottle (ringed evenly round the pool they were a scatter with a bottle in it), so the data stacks lie together; the pool itself lies a run of ink from the mouth along that line (`RUN`), where the ink stopped, since placed by its domains alone it lay wherever their mean fell, and on the tall plate that was half the column below the bottle. The louder is the spill: the heavy blots are hardly pulled and the lightest almost all the way and a hashed span past, since in a splash the smallest drops fly farthest (pulled evenly, the ink's outline was a lens with an even margin, which is a frame), the paper each keeps round it is hashed and grows with its distance from the mouth, and is wider again behind the mouth than ahead of it, and the lightest are carried on a little way along the line the ink went out (`DRIFT`), so the spill is tight round its source, thins out to the last drops and puts less behind the bottle than in front: packed evenly they are a chart, and evenly all round they are a spill with no story. A blot keeps clear of the column's edge by a margin and two fifths of its own radius, so what crosses the edge is spatter and not a blot sliced flat, which prints the frame. The geometry is `src/lib/drops.ts` in plate units (one unit a pixel at the column's full width, `--u` in CSS through a container query, so the plate scales with the column and nothing reflows), laid out twice: the wide plate, 720 by 460, for the column, and the tall one, 360 by 760, below `sm`, at `--xp` and `--yp`, so a phone gets the same spill stood on end (it had a cloud wrapped in rows, and three critics read the rows). Each is a spiral out from where the blot wants to be, heaviest first, leaning with the plate, and the test is the proof that no blot touches another on either plate heavier than the real one and that a blot lies on its domain's side. A plate with no room throws at build. The ink is a fragment shader on a canvas under the logos (`src/lib/ink-shader.ts`): every blot a distance field whose body is a drop longer along its bearing from the pool than across by a seeded share, from round to nearly twice as long as wide with most nearer round, and by more the farther it flew (the stretch keeps the area, or a row of drops merged into a chain; the pool itself hardly, or its ring of words would not fit), lopsided by four harmonics with seeded weights (a little stronger on a small blot, faint on a big one, so no two are the same species, and never so strong that a drop is a pick with its point wherever the seed put it, since the stretch is what says which way it flew), with a lobe or three on the far side from the pool where the ink splashed on most of them, broad and low because a sharp one is a speech bubble's tail, which are the rim's real events, with long smooth arc between them, under a slow crust of two noises (a few broad swells round the rim, sampled on a circle so the rim closes without a seam, and a broader lean; a rim of small even lobes all the way round read as crenellation) that the big blots carry and the small ones barely, the warp growing at two thirds the pool's rate as it opens so the words get a clean field and the rim stays wet, merged softly so two that come close join with a neck, a thin bent thread of a tail pointing back toward the pool the way a comet's says where it came from (a forensic tail points the other way, and nobody read it), its length and bend its own, off a third of the small ones (a thread, not a wedge, or the blot is a speech bubble), up to three specks of spatter thrown a radius or so clear of most, on the far side for a drop and all round for the pool, fewer and smaller the more the blot lies behind the mouth, each capped at a speck (the heaviest blot's largest read as a blot with no logo; a spill is a pool and what it threw, a plate of pools is a chart), a trail of six specks on past the pool along the line the ink came in on, each smaller and farther than the last, with one thin wandering thread from the pool's rim out to the fourth, the one line that says the ink flew, a crisp edge and a thin dark bank at the rim where the ink dried, narrow with a sharp inner edge the way a dried ring is (graded in over four units it read as a bevel on a pebble), and the body lighter the farther from the pool, since the spill dried from its edges in. One ink for every blot: the heavy ones printed a shade deeper read as a second layer, and the size carries the weight; the drying is one gradient across the spill, not a step per blot. A blot's own draws (its turn, its stretch, its lobes, whether it has a tail and where the tail points) come from `draw()` in the shader, its seed times an irrational per purpose; the sin hash fed the seed put nearly every tail on one bearing. The outline does not move with time: a rim that drifted under the pointer read as distortion, not as ink. Nothing outside the edge and no light on the body: a fibre feather, a wash halo, a print ghost and a sheen were each tried and each read as a shadow or a pebble, a broad vignette read as a bevel, and the chart has none of those. The blots are buttons over the canvas at `--px`, `--py` and `--R`, which the script writes every frame it is busy and never otherwise; the plate renders once and sleeps until something moves. When a quarter of the plate is in frame it pours first, the tongue running from the mouth to where the pool will lie over half a second (`POUR`; the run's length is `u_poured` of the way, the skid and the trail come with the pool, `u_landed`), then lands the blots out from the pool, each lagging by its distance and a hashed hair, so the accident happens once rather than being drawn already over; the buttons are hidden until then so a logo never prints on bare paper. Under reduced motion every spring jumps and the waves stop. Without WebGL the plate is `is-flat` and the CSS prints the disc, which still opens. Shoot the shader on a real GPU (headed Chrome), never on SwiftShader: SwiftShader returns zero for `atan(0, 0)` where the GPU returns NaN, and the pool printed at three times its size on every Mac while every harness shot was clean. A twelve-point `clip-path` and an SVG goo filter were the first plate: it looked like beads, and a blot cannot ripple under a filter. A ruled frame with a key and a caption in its corner, under a heading and a line of extent, was the second: an exhibit, and the ink is meant to look spilled. The plate replaced an index of sheets, a line per sheet with its title, core marks, years and a span bar, which was usable and carried far too much for the one thing the top of the page is for. Before that a plate of numbered boxes on a year grid, which read as a Gantt chart.Each series also has a running head (`.series__pin`): a zero-height sticky element after the head that sits level with the first sheet's top tick and then rides in the instrument gutter, hung just above the depth scale in the rail's column, with a small crest and the code, for as long as the series is in frame. Its position reads the same `--gutter-offset` and `--gutter-width` the depth rail is placed with, and like the rail it only prints from 1024px up. The crest markup is `Crest.astro`, used by the head's inset and the pin, sized through `--crest` on whatever `.crest` container holds it. Under the frame on the left is the source stamp: the GitHub mark and the repository path when the entry has a `repo`, otherwise a lock in a dashed box in the same slot reading "source private", so `repo` means a public repository and nothing else. No brand or company names in an entry, ever, with two exceptions: a project whose site is public can carry a `preview`, and the survey inset then names the host it links to; and the publisher of a series is named once, in `orgs.ts`, never in an entry. Client work stays unnamed. The inset (`.sheet__shot`) is a screenshot, not an iframe, because one of the two live sites sets `frame-ancestors` and refuses to be embedded, and a live frame would load someone else's page for every visitor. `scripts/shoot-previews.sh` takes the shots with a pinned `npx playwright-core` at whatever viewport each site fills; Astro converts them at build time, so shoot large and let `astro:assets` size them. The viewport in that script is the composition: the inset prints the shot whole and crops nothing, so a site whose hero runs edge to edge is shot wide and one that is a single card is shot narrow, and the two insets do not share a proportion. At rest the shot is a luminosity duotone over the frame's fill, with a per-entry `tone` aiming its value at the plate beside it; the real colours come back on hover or keyboard focus. The caption is the host and the date the shot was taken: it is a survey, not a window, and a stale one has to say so. Re-shoot and bump that entry's `preview.surveyed` in the same commit.
- Reusable component classes (`.card`, `.tag`, `.btn-primary`, `.rail`, `.portrait-inset`, `.waterline`, `.depth-rail`, `.seabed`) live in `@layer components`. Use Tailwind utilities inline for layout/spacing. No CSS modules, no styled-components. The `.prose` overrides are the exception and sit unlayered at the end of the file, because the typography plugin emits `.prose` into the utilities layer, which outranks `components` whatever the selector; moving them back into the layer loses every one of them silently.
- Border radius is `2px` everywhere. Transitions: `0.15s ease` for color/text, `0.2s ease` for background/border, `0.4s ease` for zone recolouring.
- Aesthetic is a nautical chart: hairlines, crop marks, soundings. **No glow shadows** — they were removed on purpose. Depth and emphasis come from hairlines and the orange rule.

### Live sea state

`Base.astro` fetches Open-Meteo Marine (no key, CORS open) for the Baltic off Rostock and sets `--sea-state` (0-1). Scroll sets `--attenuation` (1 at the surface, 0 at 48m) and `--depth-pct`, both measured from the waterline, not the top of the document: the paper is the surface and the gauge reads 0 m until the waterline has left the frame. If the fetch fails the CSS defaults hold and readouts stay `--`, so never make layout depend on the response.

`--sea-state` drives how rough the water looks, never whether it looks like water: a calm Baltic reports about 0.08, so every amplitude needs a floor under it. Every animation must also survive `prefers-reduced-motion` — either multiply the moving term by `var(--motion)` (which the query zeroes) or set `animation: none` in the query, whichever the keyframe allows. Shape is not motion: the waterline keeps its live amplitude when the travel stops.

### Where the water moves

Three layers, animated with transform and opacity only so they stay on the compositor. Motion is graded by depth, which is also the rule for anything added later.

- **Paper (`.zone-surface`) is deliberately still.** It is ink on a chart, and holding it still is what makes the water below read as moving. Do not animate it.
- **`.waterline` carries two swell trains** at incommensurate periods, and the far one is the edge of the paper: its SVG holds a paper fill above the profile, so the orange line is where the chart ends and the near train runs in the water below it, going under the paper wherever it rises past the edge. The layer's gradient is water only. The trains run so their crests drift in and out of phase instead of looping. Each train is two tiles wide and travels exactly one tile per cycle (`translateX(-50%)`), which is what makes the loop seamless at any viewport width. The profiles are sums of sines with **integer** wave numbers over the tile, so `y(0) === y(TILE)` by construction — keep them that way or the wrap will jog. Soundings are printed chart data and stay put; the sea moves under them.
- **`.zone-deep::before` has a slow undertow**, further and slower than anything above, with a paired opacity breath standing in for light through moving surface water. Its `inset` is negative so the drift never drags an unpainted edge into view.
- **`.drift` carries marine snow and the contacts.** See below.
- **`.shallows` is the swell itself.** Nothing up there travels; the ceiling does, and the one thing floating in it rides and heels on its mooring. See below.

`data-zone` and `data-zone-low` on `<html>` track which zone sits behind the top and bottom of the fixed rail. Fixed-position chrome that crosses the waterline needs one of these, not a static colour. Below 1024px the rail condenses to the fixed depth badge in the bottom-left corner, and a phone has no gutter to keep it clear of the content, so it yields: an observer in `DepthRail.astro` watches every `.portrait-inset__frame` against the bottom tenth of the viewport and gives the badge `is-clear` while one is there, which parks it off the edge with a stub of its frame left to hold the slot (the slide is times `--motion`; under reduced motion it fades instead). Chrome does not print over a plate.

### The water column (`Drift.astro`)

Two fields of marine snow on `.drift::before` / `::after`, plus twenty species, one component each in `src/components/figures/`, mounted in `<template>` elements by `Drift.astro` (which keeps the `SPECIES` table and the spawning script). Snow is what makes the deep read as water with volume instead of a dark background, so keep both fields: one alone reads as dust on the lens. Each field falls **exactly one tile** per cycle (`background-size` y == the keyframe's `translate3d` y) or the loop seams, and `top` overhangs by more than the longest fall so it never drags an unpainted edge into frame.

The layer is `position: fixed` and costs one viewport however long the page runs. It sits at `z-index: -1` inside `.zone-deep`: above the zone background and the contours, behind every word. The whole layer is gated to `opacity: 0` while `data-depth` is `dry`, because it is painted after the paper and would otherwise snow indoors. That gate reads `--lit`, so the narrow-screen rule can pull the layer back without out-specifying it.

**Contacts are spawned in the browser, not authored in CSS.** The `SPECIES` table carries band, mode, size, lane, duration, settled opacity and reaction; the script clones a template, assigns a lane and a heading, and removes the contact on `animationend`. Three rules keep it from reading as a loop: no species may be live twice at once, the last three species seen are excluded from the next draw (falling back to anything not live when a band has nothing fresh left), and lanes stay 15% apart (risers exempt both ways). A crossing is not a rail either: `--dy` (set per contact) is the height gained or lost over the run, and risers lean by `--dx`. Heading is rolled for every contact, standing ones included. The lane rule takes the roomiest of eight draws rather than the last one — where two species share most of their band, using whatever the final throw happened to be puts them in one lane.

**Cap is two contacts, one under 760px, and the water is meant to be mostly empty.** Three read as a crowd: a sighting only counts as one if it is alone, and the twenty species are there so that no two visits are alike, not so that a lot happens at once. Same rule for the marginalia on the paper and the soundings printed across it, density is the thing this design gets wrong first.

**A band stocks on arrival, seeded.** Leaving it to the spawn chain put the first contact up to nine seconds out, and a crossing then spends its first quarter beyond the left edge before it is visible at all, so a band you scrolled into stayed empty for half a minute and the water appeared to start at whatever scroll height you happened to stop at. `stock()` fills to the cap on every band change and at load, and `spawn(true)` gives the animation a negative delay so the contact is already in frame. The fractions passed to `lead()` are the part of each keyframe that is actually on screen; they need recomputing if the `drift-cross-*` or `drift-rise` distances change.

Every figure in the library is **drawn facing right**, and heading is `--flip` on `.drift__body`. That is the only reason nothing swims backwards — it is one rule in one place rather than a fact about twenty drawings. Any new figure must face right too. Facing right includes the rigging: a towed body goes where its cable goes, so the towfish's warp leaves from the nose, and an anglerfish's lure hangs ahead of the jaws. Both were drawn off the wrong end once. The poke heel is multiplied by `--flip` as well, or a left-facing fish fleeing upward puts its nose down. Anything with a living reaction (not `ping`) gets `is-alive`, a slow bob on the drawing at its own period; machines hold their trim.

`data-depth` on `<html>` (written in `Base.astro`) picks the band: `dry | shallow | mid | abyss`. It stays `dry` until the waterline's bottom leaves the top of the frame — the layer is fixed, so anything sooner swims over the paper — and the three bands then split **what is left after that point** into even thirds. Do not band on raw scroll progress: a page with a tall surface block spends a third of its scroll dry and leaves the shallow band a sliver.

Poking is proximity, not hit testing: the layer takes no pointer events, so `pointermove` measures against each live figure's box (an ellipse test, not a radius, or a 40px probe on a 300px wire answers from across the page). Reactions are per species and there are five: `flee`, `lunge` (the anglerfish comes to look), `pulse`, `jet` (ink), `ping` (machines answer instead of running). `.drift__body` transitions out in 0.42s and back in 2.4s — the dart is the reaction, the long drift back is it settling.

Colour is load-bearing: `--color-foam` is telemetry and only telemetry (the ROV lamps, the CTD sensor, the tagged fish's ping, the barreleye's eyes). `--color-buoy` is the one warm mark — the anglerfish lure and the oarfish crest, which sit two bands apart and never share a frame.

Figures are drawn hairline and unfilled, like a sounder or a field notebook records something rather than how an illustration pictures it. Anything with a curve comes out of `src/lib/curves.ts` (`spine` → `shell`/`ridge`/`rays`), because hand-fitting a silhouette is how the manta that used to swim here kept coming out an umbrella, and how the dumbo octopus that replaced it came out a lamb. If a new figure has parts that must line up, sample them from one source. Two things about the library that cost several attempts each: `at()` is a smoothstep and goes flat at every control point, so a profile with more than three points comes out faceted or beaded (the bells, the herring's snout, the sail's leading edge); `fair()` is the Hermite for a profile that has to carry slope through its points, and sampling a profile at exactly the spine's own steps sidesteps it. And `shell()` closes both ends with a straight wall, so a figure whose outline has to stay open (a frayed oarfish tail, a torn hull, a peduncle the tail runs through) samples both edges from one profile with a local helper instead. The rule is one source per figure, not one library call.

Six failure modes have each cost more than one attempt, and most of them are a shape that is *too regular* for what it depicts:

- **Symmetry reads as manufacture.** A rock spire zigzagged evenly on both sides is a fir tree; the smoker only became a smoker when its wobble scaled with the local girth (a fixed wobble facets a wide base and bites a narrow head), the crater went asymmetric and a live spire branched off the flank beside a dead stump. Living and geological things are lopsided. Built things are not: a sea chest keeps its straps evenly spaced.
- **A closed rectangle is a box.** The ROV read as a briefcase until the frame became posts with the far posts ghosted through open bays and one diagonal, the fittings drawn lighter than the frame, and the skids two runners instead of one full-width plinth. You must be able to see through an open frame.
- **Tapering both ends of a body loses the front.** The oarfish read as swimming backwards for exactly this reason, the turtle read as a surfboard, and the DSV faced the wrong way while its stern tapered like a nose. Give the head end its own profile: blunt, deeper, or walled.
- **A join that has to be hidden should not be drawn.** The carapace is two open edges rather than `shell()`, because the closing wall across the front cut through the head. Where a part emerges from another, leave the outline open and let them overlap.
- **An even body on legs is furniture.** The tripod fish read as a table until the body became a fish (flat back, real belly, pinched peduncle) and the tail fork closed onto the aft stilt, so the leg is visibly the animal's own tail. Anything that stands needs its legs to belong to it.
- **A line ruled across an unfilled outline does not bury it.** The wreck floated on its seabed line until the hull outline was cut where it crosses the mud and the buried run drawn dashed and light. The same rule runs the other way on paper: where a body passes in front of a line, the paper knock-out must be barely wider than the line, or the body reads as broken rather than as passing behind.

### The surface (`Shallows.astro`)

The `dry` gate above is one screen of water long, and this is what fills it. `.shallows` is `position: absolute` at the top of `.zone-deep` rather than fixed: nothing here is passing through, it is moored to the surface or riding on it, so it is drawn once and scrolls away behind you.

**Two elements, and it should stay that way.** A ceiling and the wave buoy. A screen of flat water is meant to read as flat water; the gradient down the page is notation first and life later, so no creatures live at the surface. A coaster and a pair of harbour porpoises were tried here and both read as ugly at the size they are actually seen, a hull from below is a submarine, and a small cetacean in profile is a fish. If the top ever needs more, the honest move is more water, not more figures.

- **The ceiling travels; nothing else does.** Two swell trains, same construction as `.waterline` (two tiles, integer wave numbers, one tile per cycle) and reusing the same `travel` keyframe. They run at different speeds so they part and meet, rather than holding the fixed gap a printed rule would.
- **The buoy rides and heels**, on `translate` and `rotate` as *independent* transform properties rather than one `transform`, so the two periods can be incommensurate and the swing never repeats. `transform-origin: 50% 100%` is the foot of the figure; the mooring leans off to one side and has faded out by then, so the pivot is never seen.
- **The snow fields are shared.** `.shallows::before` / `::after` carry the same two fields as `.drift`, because this layer is the only thing painting the stretch the fixed one is gated off for, without them that screen is a dark rectangle. They hand over rather than overlap: the moment `data-depth` leaves `dry` the fixed layer lights and these go to 0.

The buoy hangs its waterline off the ceiling at 40px with `top: calc(40px - var(--w) * k)`, where `k` is the waterline's y divided by the **viewBox width**. The height cancels out of that ratio, so one constant holds at every size. Changing a viewBox means recomputing `k`.

Two traps, both already paid for:

- **`mask-image` clips to the border box.** `mask-clip` defaults to `border-box`, so a mask on a figure silently deletes anything positioned outside it, which is where a label would sit. The mooring line fades through an SVG `linearGradient` on its own stroke instead. The mask on `.shallows` itself is fine: everything is inside that box.
- **`Drift.astro`'s script is inline, not a module.** `define:vars` implies `is:inline`, so it runs where it sits in the document, before this component exists. The poke system collects the moored figures on the first `pointermove` rather than at startup. Anything else in that script that reaches out of `Drift.astro` needs the same treatment.

The buoy reuses `.drift__figure` / `.drift__body` and carries `data-poke`, which is all it takes to join the water column's poke system. `data-jitter="lo hi"` sets the left range, rolled in the browser; it is held back off the right edge by its own width, because the layer clips and a figure with its nose cut off reads as a bug.

### Chart marginalia (`ChartMarks.astro`)

Charts carry two registers and so does this: the notation (variation rose, wreck symbol, tidal diamond, GNSS note, a three-bearing fix on Berlin with its cocked hat) and everything cartographers drew in the empty water to sell the chart (a sea serpent wrapped round a telegraph cable, a fleet action, somebody's cache). Nine marks, one component each in `src/components/marks/` (the ship rig is `Ship.astro`, rendered by both ship marks), three printed per visit in three of six margin slots, picked in the browser. One is pinned (`data-mark-pinned`, the tall ship "under sail 2018-19", the owner's own passage) and prints every time; the other two are drawn from the bag. Marginalia is what a cartographer put in the *empty* water; five left no empty water.

Paper does not move, so none of it animates. It is hidden below 1180px, where the content column takes the whole page and there is no margin to print in.

A mark prints 118px wide, so each of the two ships in the fleet action lands at about 30px and the passage mark's ship at about 43px. At that size a suit of sail has to be **one bellied shape per mast** with the yards ruled across it: six separate sails read as crates stacked on a raft, and thin arcs hung on spars read as bunting. One shape per mast was necessary but not sufficient: the bellied foot on each sail, spars that run out past the cloth, and a few lines of standing rigging are what finally made it canvas. The sea chest read as a barn until the lid overhung the body with a lip and the sand became a bank rather than a horizon. Size the detail to where it is seen, not to the viewBox.

### The chart table (`src/writer/`)

The blog is written at `/write`, which exists only under `npm run dev`. `integration.ts` injects the route and mounts the `/__writer/` API when the command is `dev`, and the header links to it under the same gate; a build has none of it. The desk autosaves to `src/content/blog/<slug>.md` 800ms after typing stops, the preview is the real `/blog/<slug>` page in an iframe, and publish flips `draft: false`, commits that one file as `post: <title>` and pushes. Publish refuses while anything else is staged.

`entry.ts` is pure (parse, serialize, validate, commit message) and has the test; `slug.ts` is split out so the browser script can import it without `js-yaml`. Frontmatter is parsed with `js-yaml` under `JSON_SCHEMA`, so an unquoted date stays the string it was typed as. Hand-edited files are fine; the slug is the filename and locks after the first save.

The API middleware refuses cross-site requests: it rejects a `Sec-Fetch-Site` other than `same-origin` or `none`, rejects an `Origin` that is not the dev server's own host, and requires `application/json` on `PUT` and `POST` so no write is a CORS-simple request. Every save writes a content file, and the dev server answers that with a full reload of every open page. The desk suppresses its own through a `vite:beforeFullReload` listener that throws, so the editor stays still and keeps the caret while the preview iframe reloads.

Drafts build in dev so they can be previewed and are skipped in a production build (`[...slug].astro`). The API runs git in the project root, so never start the dev server with `--host` while the writer is mounted.

## Search

Pagefind indexes the built site. CI runs `npx pagefind --site dist` after `astro build`. Search will not work in `npm run dev` without a manual index step, so don't debug "missing search" by editing code — build first.

## Deploy

GitHub Pages via `.github/workflows/deploy.yml` on push to `master`. CI: `npm ci` → `npm run build` → `npx pagefind --site dist` → deploy. Custom domain `marcel-heidebrecht.de` is set via `public/CNAME`.

## Commit messages

Never add `Co-Authored-By: Claude ...` trailers to commit messages. No Claude attribution in commits at all.
